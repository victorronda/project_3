import React from 'react'
import { withAuth } from '../context/AuthProvider'

const MainAdmin = () => {
    return (
        <div>
            
        </div>
    )
}

export default withAuth(MainAdmin)
